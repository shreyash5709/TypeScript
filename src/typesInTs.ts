let drink = "coke"

let cup = Math.random() > 0.5 ? 10 : 5 // type inferrencing means auto show which type data is stored

let channelName = "chaiaurcode"

let chaiFlavour: string = "massala chai" // annotation means manualy defines data type
chaiFlavour = "adrak chai"